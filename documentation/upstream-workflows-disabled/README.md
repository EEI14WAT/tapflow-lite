# Disabled upstream publishing workflows

These files originated from Klick'r / Smart AutoClicker upstream release infrastructure and are retained only for reference. GitHub executes workflows only from `.github/workflows`, so files in this directory are inactive.

TapFlow Lite v0.1.0 is built, signed, and published manually. No signing key, password, certificate, or other secret may be added to this repository. Do not re-enable these workflows without a dedicated security, licensing, privacy, and release-process review.